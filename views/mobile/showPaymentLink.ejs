<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Parcel Status</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f8f9fa;
    }

    .card {
      border-radius: 16px;
      border: none;
      margin: 2rem auto;
      background-color: #ffffff;
    }

    .form-label {
      font-weight: 500;
    }

    .btn-primary,
    .btn-success {
      transition: transform 0.2s ease;
    }

    .btn-primary:hover,
    .btn-success:hover {
      transform: scale(1.02);
    }

    #paymentLink {
      font-size: 0.95rem;
      background-color: #f1f3f5;
    }

    .whatsapp-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }

    @media (max-width: 576px) {
      .card {
        margin: 1rem;
        padding: 1.5rem !important;
      }
    }
  </style>
</head>

<body>

  <%- include('../partials/navbar') %>

  <div class="container d-flex justify-content-center align-items-center">
    <div class="card p-4 shadow" style="max-width: 400px; width: 100%;">
      <h5 class="mb-3 text-center">Send Payment Link</h5>
      <p class="text-center text-muted">Ask the receiver to complete payment for parcel <strong><%= parcel.customId %></strong>.</p>

      <div class="mb-3">
        <label class="form-label">Payment Link</label>
        <input type="text" class="form-control" id="paymentLink" value="<%= paymentLink %>" readonly>
      </div>

      <button class="btn btn-primary w-100 mb-2" onclick="copyLink()">📋 Copy Link</button>

      <a class="btn btn-success w-100 whatsapp-btn" target="_blank"
        href="https://wa.me/<%= parcel.receiverPhone %>?text=Hi%20<%= parcel.receiverName %>,%20please%20complete%20your%20payment%20for%20the%20parcel:%20<%= encodeURIComponent(paymentLink) %>">
        💬 Send via WhatsApp
      </a>

      <a href="/mobileDashboard" class="btn btn-outline-secondary w-100 mt-3">⬅️ Done</a>
    </div>
  </div>

  <script>
    function copyLink() {
      const linkInput = document.getElementById('paymentLink');
      linkInput.select();
      document.execCommand("copy");
      alert("Link copied to clipboard!");
    }
  </script>
</body>

</html>
