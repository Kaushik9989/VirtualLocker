<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Account Merge Request</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #f0f4ff, #e2ecf5);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .card {
      border: none;
      border-radius: 1rem;
      background-color: #fff;
    }

    .card h4 {
      font-weight: 600;
      color: #2c3e50;
    }

    .card p {
      color: #555;
    }

    .btn-primary {
      background-color: #3b82f6;
      border-color: #3b82f6;
      font-weight: 500;
    }

    .btn-primary:hover {
      background-color: #2563eb;
      border-color: #2563eb;
    }

    .btn-outline-secondary {
      font-weight: 500;
    }

    .highlight {
      color: #1d4ed8;
      font-weight: 600;
    }

    .icon {
      font-size: 2rem;
      color: #0d6efd;
    }
  </style>
</head>

<body>

  <%- include('partials/navbar') %>

  <div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow p-4" style="max-width: 500px; width: 100%;">
      <div class="text-center mb-3">
        <div class="icon mb-2">📱</div>
        <h4>Phone Number Already Linked</h4>
      </div>
      <p class="text-center">
        The number <span class="highlight">(<%= phone %>)</span> is already connected to another account.
      </p>
      <p class="text-center">
        Do you want to claim ownership and merge both accounts into one?
      </p>

      <form action="/mobile/merge-confirm" method="POST" class="d-grid gap-2 mt-3">
        <input type="hidden" name="confirm" value="yes" />
        <button type="submit" class="btn btn-primary">Yes, Merge My Accounts</button>
      </form>

      <form action="/mobile/link-phone" method="GET" class="d-grid gap-2 mt-2">
        <button type="submit" class="btn btn-outline-secondary">No, Go Back</button>
      </form>
    </div>
  </div>

</body>

</html>
