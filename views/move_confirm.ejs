<% layout("/layouts/boilerplate") %>
<style>
  @media (min-width: 768px) {
    .helli {
      margin-left: 200px;
    }
  }</style>
<div class="helli">
<div class="container mt-4">
  <h4 class="mb-3">🚚 Select Delivery Partner</h4>

  <p><strong>From:</strong> <%= fromPincode %> → <strong>To:</strong> <%= toPincode %></p>

  <form action="/parcel/<%= parcel._id %>/move/confirm" method="POST">
    <% deliveryOptions.forEach(opt => { %>
      <div class="card mb-2">
        <div class="card-body d-flex justify-content-between align-items-center">
          <div>
            <strong><%= opt.courier_name %></strong><br/>
            Charges: ₹<%= opt.rate %><br/>
            ETA: <%= opt.etd %>
          </div>
          <input type="radio" name="courier_code" value="<%= opt.courier_company_id %>" required />
        </div>
      </div>
    <% }) %>

    <button type="submit" class="btn btn-primary w-100 mt-3">Confirm & Proceed</button>
  </form>
</div>
</div>