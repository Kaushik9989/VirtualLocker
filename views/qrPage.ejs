<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Parcel QR Code</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background: #f8fafc;
      padding: 20px;
      text-align: center;
    }
    img {
      max-width: 300px;
      border: 2px solid #ddd;
      border-radius: 8px;
      padding: 10px;
      background: white;
    }
    .btn-back {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <h4 class="mb-3">QR Code for Parcel</h4>
  <img src="<%= parcel.qrCodeUrl %>" alt="QR Code" />
  <p class="mt-2 text-muted">Access Code: <strong><%= parcel.accessCode %></strong></p>
  <a href="/dashboard" class="btn btn-secondary btn-back">
    <i class="fas fa-arrow-left me-1"></i> Back to Dashboard
  </a>
</body>
</html>
