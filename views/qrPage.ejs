<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="manifest" href="/manifest.json">
<meta name="theme-color" content="#0f172a">
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-DPJJFZLL9L"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-DPJJFZLL9L');
  </script>
  
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Parcel QR Code</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet"/>
  
  <style>
    body {
      background: linear-gradient(to bottom right, #dff1ff, #ffffff);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      color: #333;
      position: relative;
    }

    .back-btn {
      position: absolute;
      top: 20px;
      left: 20px;
    }

    .qr-card {
      background: #ffffff;
      border-radius: 20px;
      box-shadow: 0 12px 30px rgba(0,0,0,0.15);
      padding: 30px 20px;
      max-width: 400px;
      width: 100%;
      text-align: center;
      animation: fadeInUp 0.5s ease;
    }

    .qr-card h4 {
      font-weight: 600;
      margin-bottom: 1rem;
      color: #333;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      font-size: 1.3rem;
    }

    .qr-img {
      max-width: 260px;
      width: 100%;
      border-radius: 12px;
      border: 1px solid #ddd;
      padding: 10px;
      background: #fff;
      transition: transform 0.3s ease;
    }

    .qr-img:hover {
      transform: scale(1.05);
    }

    .info-block {
      margin-top: 1rem;
      text-align: left;
      font-size: 0.95rem;
      color: #555;
      background: #f8f9fa;
      border-radius: 10px;
      padding: 10px 15px;
    }

    .info-block strong {
      color: #000;
      font-weight: 600;
    }

    .btn-group {
      display: flex;
      flex-direction: column;
      gap: 0.6rem;
      margin-top: 1.5rem;
    }

    .btn-custom {
      padding: 0.6rem 1.2rem;
      font-size: 0.95rem;
      border-radius: 8px;
      transition: all 0.2s;
    }

    .btn-custom i {
      margin-right: 6px;
    }

    .btn-custom:hover {
      transform: translateY(-2px);
      opacity: 0.9;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>
  <!-- Top Left Back Button -->
  <div class="back-btn">
    <a href="/dashboard" class="btn btn-dark btn-sm">
       Visit DropPoint Systems
    </a>
  </div>

  <!-- QR Card -->
  <div class="qr-card">
    <h4>
      <i class="fas fa-box-open text-primary"></i> Parcel QR Code
    </h4>
    <img src="<%= qrImage %>" alt="QR Code" class="qr-img"/>
    
    <div class="info-block mt-3">
      <div><i class="fas fa-lock"></i> Locker: <strong><%= parcel.lockerId || 'N/A' %></strong></div>
      <div><i class="fas fa-cube"></i> Compartment: <strong><%= parcel.compartmentId || 'N/A' %></strong></div>
      <div><i class="fas fa-key"></i> Access Code: <strong><%= parcel.accessCode %></strong></div>
    </div>

    <div class="btn-group">
      <a href="https://www.google.com/maps/dir/?api=1&destination=<%= parcel.lockerLat %>,<%= parcel.lockerLng %>" target="_blank" class="btn btn-primary btn-custom">
        <i class="fas fa-location-arrow"></i> Get Directions
      </a>
    </div>
  </div>
  <script>
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js')
      .then(() => console.log('SW registered'))
      .catch(err => console.error('SW registration failed', err));
  }
</script>

</body>
</html>
