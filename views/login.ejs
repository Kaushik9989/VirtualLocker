<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Drop Point – Login</title>

  <!-- Bootstrap & Font Awesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />

  <style>
    body {
      background: linear-gradient(to bottom right, #e0f2fe, #fffaf0);
      font-family: "Poppins", sans-serif;
    }

    .card {
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
      background: #ffffff;
    }

    .form-control {
      border-radius: 10px;
      padding: 10px 14px;
    }

    .form-control:focus {
      border-color: #2563eb;
      box-shadow: 0 0 8px rgba(37, 99, 235, 0.2);
    }

    .btn-orange {
      background-color: #2563eb;
      color: white;
      border-radius: 10px;
      font-weight: 500;
    }

    .btn-orange:hover {
      background-color: #1d4ed8;
    }

    .btn-google {
      border: 1px solid #ccc;
      border-radius: 10px;
      background: white;
      color: #333;
      font-weight: 500;
    }

    .btn-google:hover {
      background-color: #f8f9fa;
    }

    .logo {
      max-height: 60px;
    }

    .feature-box {
      padding: 12px;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      background: white;
      text-align: center;
      font-size: 0.8rem;
      font-weight: 500;
      color: #475569;
    }

    .feature-box i {
      display: block;
      font-size: 18px;
      color: #2563eb;
      margin-bottom: 4px;
    }
  </style>
</head>

<body class="d-flex align-items-center justify-content-center min-vh-100">

  <div class="container px-3">
    <div class="row justify-content-center">
      <div class="col-lg-6 col-md-9">
        <div class="card p-4 p-md-5">
          <!-- Logo -->
          <div class="text-center mb-4">
            <img src="/uploads/droppointlogo.png" alt="Drop Point Logo"  style="background-color: #333; border-radius: 20px;" class="logo mb-3" />
            <h2 class="fw-bold text-primary">Welcome to Drop Point</h2>
            <p class="text-muted small">Smart parcel delivery made simple</p>
          </div>

          <!-- Feature Highlights -->
          <div class="d-flex justify-content-between mb-4">
            <div class="feature-box w-100 mx-1">
              <i class="fas fa-bolt"></i>Fast Delivery
            </div>
            <div class="feature-box w-100 mx-1">
              <i class="fas fa-shield-alt text-success"></i>Secure
            </div>
            <div class="feature-box w-100 mx-1">
              <i class="fas fa-check-circle text-warning"></i>Reliable
            </div>
          </div>

          <!-- Alert -->
          <% if (error) { %>
            <div class="alert alert-danger d-flex align-items-center">
              <i class="fas fa-exclamation-circle me-2"></i>
              <%= error %>
            </div>
          <% } %>

          <!-- Google Login -->
          <div class="d-grid mb-2">
            <a href="/auth/google" class="text-decoration-none">
              <button type="button" class="btn btn-google d-flex align-items-center justify-content-center w-100">
                <img src="https://www.google.com/favicon.ico" alt="Google" style="width: 20px; height: 20px;" class="me-2" />
                Sign in with Google
              </button>
            </a>
          </div>

          <div class="text-center text-muted mb-1">OR</div>

          <!-- Phone Login -->
          <div>
            <form method="POST" action="/otpLogin">
              <div class="mb-2">
                <label for="phone" class="form-label">Phone Number</label>
                <div class="input-group">
                  <span class="input-group-text"><i class="fas fa-phone"></i></span>
                  <input type="text" class="form-control" id="phone" name="phone" placeholder="+91xxxxxxxxxx" required />
                </div>
              </div>
              <div class="d-grid">
                <button type="submit" class="btn btn-orange">
                  <i class="fas fa-sign-in-alt me-2"></i>Login with Phone
                </button>
              </div>
            </form>
          </div>

          <hr class="my-4" />

          <p class="text-center mb-1">
            Don’t have an account?
            <a href="/register" class="fw-semibold text-decoration-none text-primary">Register</a>
          </p>
          <p class="text-center">
            Admin? <a href="/admin/login" class="fw-semibold text-decoration-none text-primary">Login here</a>
          </p>

          <!-- Terms and Trust Section -->
          <p class="text-center small text-muted mt-3">
            By continuing, you agree to our <a href="#" class="text-decoration-none">Terms</a> and <a href="#" class="text-decoration-none">Privacy Policy</a>.
          </p>
          <div class="d-flex justify-content-center gap-3 text-muted small">
            <span><i class="fas fa-shield-alt me-1"></i>Secure</span>
            <span><i class="fas fa-envelope me-1"></i>No Spam</span>
            <span><i class="fas fa-thumbs-up me-1"></i>Trusted</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap Script -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
