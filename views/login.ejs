<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Drop Point – Login</title>
  
  <!-- Bootstrap + Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet" />
  
  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet"/>

  <style>
    body {
      background: linear-gradient(135deg, #ff7e5f, #feb47b);
      font-family: "Poppins", sans-serif;
    }

    .card {
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
      background: #ffffff;
    }

    .form-control {
      border-radius: 10px;
      padding: 10px 14px;
    }

    .form-control:focus {
      border-color: #ff7e5f;
      box-shadow: 0 0 10px rgba(255, 126, 95, 0.3);
    }

    .btn-orange {
      background-color: #ff7e5f;
      color: white;
      border: none;
      border-radius: 10px;
      padding: 10px;
    }

    .btn-orange:hover {
      background-color: #e56247;
    }

    .btn-google {
      border: 1px solid #ccc;
      border-radius: 10px;
      background: white;
      color: #333;
      font-weight: 500;
    }

    .btn-google:hover {
      background-color: #f4f4f4;
    }

    .logo {
      max-height: 60px;
      margin-bottom: 20px;
    }

    .login-section h4 {
      font-size: 1.1rem;
      color: #ff7e5f;
    }

    @media (min-width: 768px) {
      .login-container {
        display: flex;
        gap: 30px;
      }
    }
  </style>
</head>

<body class="d-flex align-items-center justify-content-center min-vh-100">

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-8 col-md-10">
        <div class="card p-4 p-md-5">
          <div class="text-center">
            <img src="/uploads/droppointlogo.png" alt="Drop Point Logo" class="logo" />
            <h2 class="mb-4 fw-bold text-orange">Login to Drop Point</h2>
          </div>

          <% if (error) { %>
          <div class="alert alert-danger d-flex align-items-center">
            <i class="fas fa-exclamation-circle me-2"></i> <%= error %>
          </div>
          <% } %>

          <!-- Google Login -->
          <div class="d-grid mb-4">
            <a href="/auth/google" class="text-decoration-none">
              <button type="button" class="btn btn-google d-flex align-items-center justify-content-center w-100">
                <img src="https://www.google.com/favicon.ico" alt="Google" style="width: 20px; height: 20px; margin-right: 10px" />
                Sign in with Google
              </button>
            </a>
          </div>

          <div class="login-container">
            <!-- Username/Password Login -->
            <div class="login-section w-100">
              <h4 class="text-center mb-3">Login with Username</h4>
              <form method="POST" action="/auth/login">
                <div class="mb-3">
                  <label for="username" class="form-label"><i class="fas fa-user me-2"></i>Username</label>
                  <input type="text" class="form-control" name="username" id="username" required />
                </div>
                <div class="mb-3">
                  <label for="password" class="form-label"><i class="fas fa-lock me-2"></i>Password</label>
                  <input type="password" class="form-control" name="password" id="password" required />
                </div>
                <div class="d-grid">
                  <button type="submit" class="btn btn-orange">
                    <i class="fas fa-sign-in-alt me-2"></i>Login
                  </button>
                </div>
              </form>
            </div>

            <!-- Phone Login -->
            <div class="login-section w-100">
              <h4 class="text-center mb-3">Login with Phone</h4>
              <form method="POST" action="/login-send-otp">
                <div class="mb-3">
                  <label for="phone" class="form-label"><i class="fas fa-phone me-2"></i>Phone Number</label>
                  <input type="text" class="form-control" id="phone" name="phone" required />
                </div>
                <div class="d-grid">
                  <button type="submit" class="btn btn-orange">
                    <i class="fas fa-sms me-2"></i>Send OTP
                  </button>
                </div>
              </form>
            </div>
          </div>

          <hr class="my-4" />

          <p class="text-center">
            Don’t have an account?
            <a href="/register" class="text-orange fw-semibold">Register</a>
          </p>
          <p class="text-center">
            Admin? <a href="/admin/login" class="text-orange fw-semibold">Login here</a>
          </p>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
