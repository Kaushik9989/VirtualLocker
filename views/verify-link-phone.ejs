<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Parcel Status - Verify Phone</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fc;
      font-family: 'Segoe UI', sans-serif;
    }

    .verify-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .verify-card {
      background: white;
      border-radius: 16px;
      padding: 2rem;
      max-width: 400px;
      width: 100%;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
    }

    .verify-card h4 {
      font-weight: 600;
      color: #343a40;
    }

    .verify-input {
      font-size: 1.1rem;
      text-align: center;
      letter-spacing: 3px;
      border-radius: 10px;
      padding: 0.75rem;
    }

    .verify-btn {
      background-color: #28a745;
      color: white;
      font-weight: 600;
      font-size: 1rem;
      border-radius: 12px;
      transition: background-color 0.2s ease;
    }

    .verify-btn:hover {
      background-color: #218838;
    }

    .alert {
      font-size: 0.9rem;
      border-radius: 8px;
    }
  </style>
</head>

<body>

  <%- include('partials/navbar') %>

  <div class="verify-container">
    <div class="verify-card">
      <h4 class="text-center mb-4">🔐 Verify Your Phone</h4>

      <% if (error) { %>
        <div class="alert alert-danger text-center small">
          <%= error %>
        </div>
      <% } %>

      <form method="POST" action="/verify-link-phone">
        <div class="mb-3">
          <label for="otp" class="form-label">Enter OTP</label>
          <input
            type="text"
            class="form-control verify-input"
            name="otp"
            maxlength="6"
            placeholder="6-digit code"
            required
          />
        </div>
        <button type="submit" class="btn btn-primary verify-btn w-100 py-2">
          ✅ Verify & Continue
        </button>
      </form>
       <p class="text-muted small mt-4 mb-0">
                  Didn’t get the code?
                <form id="resendForm" action="/resend-login-otp" method="POST" class="d-inline">
                  <input type="hidden" name="resend" value="true" />
                  <button type="submit" class="btn btn-link p-0 m-0 align-baseline text-primary resend-link">
                    Resend OTP
                  </button>
                </form>
                </p>
      <div id="alertBox" class="mt-3"></div>
    </div>
  </div>



</body>

</html>
